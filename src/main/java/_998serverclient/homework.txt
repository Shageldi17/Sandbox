Создать класс Task. С названием задачи и c описанием (поля)
 Создать конструктор для инициализации этих полей.
 Создать геттеры и сеттеры.
 Создать несколько Task в классе Main, попробовать воспользоваться
  геттерами и сеттерами.
  Напечатать объекты Task в консоли

Создать HashSet для хранения Task
положить туда несколько Task (в том числе с одинаковыми полями)
распечатать HashSet (повторения будут!)
переопределить equals & hashcode
очистить HashSet (clear())
опять положить туда те же Task (в том числе с одинаковыми полями)   (чтобы посмотреть на эффект equals и HashCode)
распечатать HashSet (повторений быть не должно!)


  Бонус: Использовать Scanner чтобы узнать в консоли название и
   описание задачи, а затем создать Task используя эти значения
+
    Set (HashSet)
        нет повторений
        нет индексов (поэтому нет порядка)
+
   SQL                  online compiler (вместо Intellij Idea)
        SELECT
        INSERT
        FROM

        CREATE
        INTO
        WHERE

+
    read about Recursion (fibonacci, factorial)

    NEXT
        github
        recursion

